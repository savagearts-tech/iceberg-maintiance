
## 🛡️ FDS 平台 Iceberg 存储维护关键特性清单 (JDBC 专家版)

| 维度 | 特性名称 | 技术实现核心 | 架构价值与风险对冲 |
| --- | --- | --- | --- |
| **治理策略** | **双重约束清理 (Dual Retention)** | `expireOlderThan(90d)` + `retainLast(5)` | **一致性保障**：在回收 3 个月前空间的同时，确保即便系统长期未更新，仍有 5 个历史版本用于 Time Travel 恢复。 |
| **治理策略** | **JDBC 事务原子化** | 废弃 `version-hint.text`，由 RDBMS 维护元数据指针 | **架构加固**：利用数据库 ACID 事务彻底解决 S3/MinIO 的物理文件锁竞争，消除跨平台（Win/Linux）兼容性隐患。 |
| **性能优化** | **手术式分区裁剪 (Partition Pruning)** | `TableScan.filter()` 精确加载特定分区元数据 | **资源管控**：将内存开销从全表量级降至分区量级，支撑 FAB 海量数据下 **100MB 级别** 的超轻量维护。 |
| **性能优化** | **定向物理路径扫描** | S3 前缀（Prefix）锁定 `/data/partition=value/` | **IO 减负**：规避对全量桶（Bucket）的无效遍历，极大缩短物理对比耗时，解决海量小文件扫描超时问题。 |
| **安全加固** | **两阶段混合清理模型** | `L1: 逻辑版本过期` -> `L2: 物理孤立比对` | **闭环清理**：通过元数据内外双重验证，精准清除因写入崩溃、事务回滚产生的“幽灵文件（Orphan Files）”。 |
| **安全加固** | **三日强制冷静期 (Grace Period)** | 物理判定逻辑：`last_modified < (Now - 3d)` | **容错保底**：物理层面避开正在进行的实时提交与 S3 最终一致性延迟，确保活跃数据绝对不被误删。 |
| **安全加固** | **URI 规范化对比 (Normalization)** | 强制执行 `s3a://` 协议组装与绝对路径对齐 | **风险防御**：解决 S3 Key 与 Iceberg Location 格式不一致导致的路径比对失效，**这是防止“全删事故”的核心屏障**。 |
| **运维工程化** | **目录级物理隔离** | 扫描范围限 `data/`，物理禁入 `metadata/` | **结构保护**：即便清理逻辑异常，也能通过物理隔离确保核心元数据 JSON 的完整，保留表的“复活”可能。 |
| **运维工程化** | **零引擎轻量化实现** | 纯 Java 实现，仅依赖 Core 库与 AWS SDK V2 | **敏捷部署**：不依赖 Spark/Flink，可作为轻量级 Agent 部署在 FAB 的边缘节点或 K8s Sidecar 中。 |

